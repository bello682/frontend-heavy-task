// // utils/chatVisibility.js
// export const shouldShowChatIcon = (currentPath) => {
// 	const isLoggedIn = !!localStorage.getItem("userId");
// 	if (isLoggedIn) {
// 		// Show in dashboard only
// 		return currentPath.includes("/dashboard");
// 	} else {
// 		// Show on landing only
// 		return currentPath === "/" || currentPath.includes("/");
// 	}
// };
